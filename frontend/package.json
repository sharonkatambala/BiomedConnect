{
  "name": "biomedconnect-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "cross-env BROWSER=chrome vite",
    "build": "vite build",
    "preview": "cross-env BROWSER=chrome vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "vite": "^7.0.0"
  }
}
